// The Fixtures page is decomposed under ./Fixtures/. This thin entry keeps the
// Inertia component name `Social/Fixtures` resolving to the folder's Index.
export { default } from './Fixtures/Index';
