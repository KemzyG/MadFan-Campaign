; cPanel / shared hosting PHP overrides (applied per-directory).
; Raise these in MultiPHP INI Editor if .user.ini is ignored.

memory_limit = 256M
max_execution_time = 120
upload_max_filesize = 32M
post_max_size = 32M
max_input_vars = 3000
